#Redondear numeros
print(round(6 / 3))
#Redondear con decimales que se quiera
print(round(6 / 3, 2)) # 2 numero de decimales que se quiere
#Convertir resultado directo a int y no sea float
print(round(6 // 3))
